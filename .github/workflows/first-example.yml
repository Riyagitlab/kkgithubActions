name: My First workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
           - name: checkout Repo
             uses: actions/checkout@v6 

           - name: List and Read file
             run: |
                echo "My Firset Github Actions"
                ls -ltra
                cat README.md

           - name: Generate ASCII Artwork
             run: cowsay -f lion "Run for cover,I am a LION ....ROAR" >> lion.txt   
             
           
             
           