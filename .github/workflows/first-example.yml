name: My First workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
           - name: Welcome message
             run: echo "My Firset Github Actions"
             
           - name: List Files
             run: ls
             
           - name: Read file
             run: cat README.md  