name: My First workflow
on: push
jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
           - name: checkout Repo
             uses: actions/checkout@v6 

           - name: List files
             run: ls -ltra  

           - name: Executing shell script
             run: ./ascii-script.sh

            